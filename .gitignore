# dependencies
node_modules/

# build output (every release build lands here; keep only the latest)
release/
release-upd/
release-upd-*/
dist/

# bundled runtime (vendored node + dsh dependency tree — regenerated
# from the npx cache by scripts/prepare-runtime.ps1 before packaging)
resources/dsh-runtime/
resources/dsh-runtime.tar.gz
resources/dsh-runtime.sha256

# logs
logs/
*.log

# electron-builder cache / debug
builder-debug.yml
*.blockmap

# OS
Thumbs.db
.DS_Store
