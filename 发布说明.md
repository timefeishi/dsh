# 发布新版本到 GitHub Releases（自动更新流程）

## 首次准备（一次性）

1. **确保本机已配置 GitHub 凭据**（用于推代码和发布 Release）：
   ```powershell
   gh auth login          # 或者
   git config --global credential.helper manager
   ```

2. **创建 GitHub Personal Access Token**（用于 electron-builder 上传 Release）：
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - 勾选 `repo` 权限，生成 token
   - 复制 token

## 每次发布新版本

### 1. 升级版本号

编辑 `package.json`，把 `"version": "1.0.0"` 改成新版本（如 `1.0.1`）。

### 2. 构建并发布

```powershell
cd C:\Users\16667\Desktop\dsh\dsh-desktop

# 方式 A：electron-builder 直接上传到 GitHub Releases
$env:GH_TOKEN = "你的token"
npm run publish        # = electron-builder --win nsis --publish always
```

electron-builder 会自动：
- 构建安装包（`DeepSeek-Harness-Setup-<version>.exe`）
- 生成 `latest.yml` 元数据
- 在 GitHub 创建 Release（tag 为版本号），上传 exe + latest.yml + blockmap

### 3. 其他设备自动更新

已安装的 app 启动时会自动检查 GitHub Releases：
- 发现新版本 → 弹窗询问"是否下载更新"
- 下载完成 → 提示重启应用完成更新
- 也可在托盘菜单手动"检查更新…"

## 手动发布（不想用 token 时）

1. 构建：`npm run dist`（产物在 `release/` 或 `release-upd/`）
2. 打开 GitHub 仓库 → Releases → Create a new release
3. Tag 填版本号（如 `v1.0.1`），标题填版本号
4. 上传三个文件：`DeepSeek-Harness-Setup-<v>.exe`、`latest.yml`、`.exe.blockmap`
5. 发布

> ⚠️ latest.yml 和安装包必须**同一次构建**生成（sha512 一致），不能混用。

## 注意

- 更新包校验 sha512：安装包与 latest.yml 必须配套
- 首次发布建议先在测试机验证"检查更新 → 下载 → 重启"完整链路
- 未签名应用在 SmartScreen 会有提示，属正常现象
